export interface PurchaseRequestItem {
  id: string;
  itemName: string;
  quantity: string;
  unit: string;
  estimatedPrice: string;
  supplier: string;
  dueDate: string;
  purpose: string;
  notes: string;
}
